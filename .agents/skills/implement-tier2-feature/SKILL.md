# Implement Tier-2 Feature

Follow `../implement-tier2-content-item/SKILL.md` first.

## Additional feature rules
1. Validate the exact class/subclass feature and granted level from the local XPHB class data.
2. Route feature unlocking through the shared advancement / capability framework.
3. Generate and block on required pending choices instead of guessing selections.
4. Update the Tier-2 class-feature or subclass-feature manifest plus the support matrix.
