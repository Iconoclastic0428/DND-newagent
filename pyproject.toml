[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dnd-character-creation-kernel"
version = "0.1.0"
description = "Deterministic D&D 2024 character creation kernel backed by 5etools data."
requires-python = ">=3.12"
readme = "docs/architecture/dnd-agent.md"
