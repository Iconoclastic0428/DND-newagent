# Shatter

This item directory is an honest blocker shell.

The local XPHB text is present in the repository mirror, but exact runtime support still needs an unattended-object damage primitive. The current runtime can resolve creature saves and damage, but it cannot yet apply exact thunder damage to nonmagical objects that are neither worn nor carried.

Cast syntax:

`/cast player-1 shatter 5 2`
